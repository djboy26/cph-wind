# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# On-demand audit report + its generator (regenerated when asked; never committed)
/reports/
/scripts/build-audit-deck.py

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
public/data/cph-segments.json

# Reviewer hand-offs applied with `git apply --3way <name>.patch`; never committed
/*.patch

# Screenshot harness (scripts/shots.mjs): report.md per run is committed, the pictures stay local
docs/renders/**/*.png
docs/renders/**/*.txt
